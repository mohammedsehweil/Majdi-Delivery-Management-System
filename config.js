/**
 * Central Configuration File
 * 
 * Change settings here once, and they'll apply to all pages.
 */

// API Base URL
window.__API_BASE = "https://hide-into-belong-contributor.trycloudflare.com/ConstructionCompany/api";


// CORS Proxy Settings
// Set to true if you encounter CORS issues and need to use a proxy
// Set to false to connect directly to your API (recommended if your backend supports CORS)
window.__USE_CORS_PROXY = true;

